No specifications.
Not a completely clear task, but I wanted to point out that in the last paragraph,
I could add a parameter to the function to get the format you specified and convert it to bytes like: Hex() to bytes.
